#include<bits/stdc++.h>
using namespace std;

int compares(string a,string b)
{
    int n=a.length(),m=b.length();
    int i=0,j=0;

    while(i<n or j<n)
    {
        
    }
}

int main()
{
    string a,b;
    cin>>a>>b;

    cout<<compares(a,b);
    return 0;
}